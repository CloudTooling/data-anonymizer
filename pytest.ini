[pytest]
# Pytest configuration for CSV Anonymizer

python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts = 
    -v
    --strict-markers
    --tb=short

testpaths = tests

markers =
    unit: marks tests as unit tests
    csv: marks tests for CSV functionality
